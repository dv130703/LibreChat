# Web server
fastapi
uvicorn[standard]
python-multipart

# HTTP client (embedding / rerank calls to the processing machine)
requests

# Vector store
lancedb
numpy
pyarrow

# Auth (verifies LibreChat's short-lived HS256 tokens)
pyjwt
python-dotenv

# Document text extraction
pypdf
python-docx
openpyxl

# Transcription (WhisperX: ASR + alignment + diarization)
# torch/torchaudio: install separately first, matched to your CUDA driver -
# see rag_server/README or the root SETUP.md. `pip install -r` alone pulls a
# CPU-only build if torch isn't already present.
whisperx>=3.1.1
pandas
scipy
pyannote.audio>=3.1
pydantic-settings>=2.4

